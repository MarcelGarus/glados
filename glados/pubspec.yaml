name: glados
description: "🍰 A property-based testing framework that tries to break your invariances."
version: 0.3.1

repository: https://github.com/marcelgarus/glados

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  analyzer: ">=0.38.0 <0.41.0"
  build: ">=1.3.0 <1.6.0"
  characters: ^1.0.0
  meta: ^1.0.0
  source_gen: ^0.9.7
  test: ^1.0.0
